
  # HTML Email Template Creation

  This is a code bundle for HTML Email Template Creation. The original project is available at https://www.figma.com/design/34xzhMdSyeLOwqE35nwP95/HTML-Email-Template-Creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  